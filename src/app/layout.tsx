import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SkillCraft - Generate AI Agent Skills from Docs",
  description:
    "Open source tool to generate SKILL.md files for AI coding agents from any documentation URL. Powered by Firecrawl.",
  openGraph: {
    title: "SkillCraft",
    description: "Generate AI agent skills from any documentation URL",
    siteName: "SkillCraft",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
